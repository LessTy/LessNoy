# 🚀 Инструкции по развёртыванию на Netlify

## Текущее состояние сайта

✅ **Полностью готовый рабочий сайт с:**
- 6 основных страниц + 404
- 3 статьи в блоге
- Контактная форма
- Полностью адаптивный дизайн
- Быстрая загрузка (Vite)

## Шаг 1: Подготовка к развёртыванию

### Локально

```bash
# Убедиться, что всё собирается без ошибок
npm run build

# Проверить, что dev сервер работает
npm run dev
```

### На GitHub

```bash
# Инициализировать git репозиторий (если ещё не сделано)
git init

# Добавить все файлы
git add .

# Первый коммит
git commit -m "Initial commit: complete website for Leshy's Hut"

# Добавить remote и push
git remote add origin https://github.com/YOUR_USERNAME/naija-connect-clone.git
git push -u origin main
```

## Шаг 2: Развёртывание на Netlify

### Способ 1: GitHub интеграция (рекомендуется)

1. Перейти на [netlify.com](https://netlify.com)
2. Нажать "New site from Git"
3. Выбрать GitHub и авторизоваться
4. Выбрать репозиторий `naija-connect-clone`
5. Настройки сборки:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
6. Нажать "Deploy site"

### Способ 2: Прямая загрузка

1. Локально собрать проект: `npm run build`
2. На Netlify: перетянуть папку `dist/` на сайт
3. Сайт будет развёрнут мгновенно

## Шаг 3: Настройка доменного имени

После развёртывания:

1. В панели Netlify перейти в "Site settings"
2. Найти раздел "Domain management"
3. Нажать "Add custom domain"
4. Ввести ваш домен (например, leshy-hut.ru)
5. Следовать инструкциям для настройки DNS

## Шаг 4: SSL сертификат

Netlify автоматически предоставляет бесплатный SSL сертификат через Let's Encrypt.

## Шаг 5: Настройка формы обратной связи

Форма на странице `/contacts` сейчас логирует данные в консоль.

### Для реальной отправки на email:

#### Вариант 1: Netlify Forms (самый простой)

1. Добавить `netlify` атрибут к форме в `src/pages/Contacts.tsx`:
```jsx
<form name="contact" method="POST" netlify>
```

2. Добавить скрытое поле:
```jsx
<input type="hidden" name="form-name" value="contact" />
```

#### Вариант 2: Использовать сервис (Formspree, EmailJS)

```bash
npm install emailjs-com
```

Затем интегрировать в форму.

## Текущая конфигурация Netlify

```toml
[build]
  command = "npm run build"
  publish = "dist"
  
[functions]
  directory = "functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

## Переменные окружения (если потребуются)

В панели Netlify: Site settings → Build & deploy → Environment

Пример:
```
VITE_API_KEY=your_key_here
VITE_API_URL=https://api.example.com
```

## Мониторинг и аналитика

### Google Analytics

1. Создать проект в [Google Analytics](https://analytics.google.com)
2. Получить ID (вид: G-XXXXXXXXXX)
3. Установить пакет:
```bash
npm install react-ga4
```

4. Добавить в `src/main.tsx`:
```jsx
import ReactGA from 'react-ga4';

ReactGA.initialize('G-XXXXXXXXXX');
ReactGA.send({
  hitType: 'pageview',
  page: window.location.pathname + window.location.search,
});
```

### Netlify Analytics

Автоматически включена для всех сайтов на Netlify Pro/Business планах.

## Проблемы и решения

### Маршруты не работают после развёртывания

**Решение**: Убедитесь, что файл `_redirects` находится в папке `public/`:

```
/*    /index.html   200
```

Vite автоматически копирует содержимое папки `public/` в `dist/`.

### Медленная загрузка

1. Оптимизировать изображения (использовать WebP)
2. Включить кэширование в Netlify
3. Использовать CDN для статических файлов

### Форма не отправляется

Проверьте:
- Атрибут `name` на форме
- Атрибут `netlify` добавлен
- Скрытое поле `form-name` присутствует

## Дополнительные улучшения

### 1. Добавить sitemap
```bash
npm install vite-plugin-sitemap
```

### 2. Добавить PWA поддержку
```bash
npm install vite-plugin-pwa
```

### 3. Настроить редирект HTTP → HTTPS
Автоматически включено на Netlify.

### 4. Добавить rate limiting для формы
Встроено в Netlify Forms.

## Команды для фронтенда

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Предпросмотр build
npm run preview

# Линтинг
npm run lint

# Линтинг с фиксом
npm run lint --fix
```

## Постоянное обновление содержимого

### Добавить новую статью в блог:

1. Создать файл в `src/content/blog/`:
```
04-new-article.md
```

2. Добавить frontmatter:
```markdown
---
id: new-article
title: Название статьи
slug: new-article
author: Леший
date: 2025-01-20
category: Категория
excerpt: Краткое описание
---
```

3. Добавить в массив `blogPosts` в `src/pages/Blog.tsx` и `blogPostsContent` в `src/pages/BlogPost.tsx`

4. Пересобрать и задеплоить:
```bash
npm run build
git add .
git commit -m "Add new blog post"
git push
```

## Контакты для тестирования

- Email: hello@leshy.ru
- Телефон: +7 (999) 999-99-99
- Telegram: @leshy_hut

---

**Сайт готов к развёртыванию! 🚀**
